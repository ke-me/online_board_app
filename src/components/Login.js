import React from 'react';
import "./Login.css";

const Login = () => {
  return (
    <section>
        <p>Googleアカウントでログインする</p>
        <button>ログイン</button>
    </section>
  )
};

export default Login;